#include <gtest/gtest.h>

#include "packet.h"

int pull_test_packet() { return 0; }

// TEST(Packet, Basic) {
//   Packet pkt(0);
//   char *payload = new char[16];
//   pkt.set_payload(payload, sizeof(payload));
//   ASSERT_EQ(sizeof(payload), pkt.get_payload_length());
// }
