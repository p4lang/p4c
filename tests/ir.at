AT_BANNER([intermediate representation unit tests])

m4_define([TEST_IR],
[AT_SETUP([$1])
 AT_CHECK([p4ctest $2], [0], [stdout], [])
 AT_CLEANUP]
)
TEST_IR([ir - dump json], [test-json])
TEST_IR([ir - opeq], [test-opeq])
TEST_IR([ir - transform], [test-transform])
