# for tests we relax a lot of constraints (mostly interested in format checks)
filter=-build/namespaces
filter=-runtime/explicit
filter=-runtime/references
filter=-runtime/printf
filter=-runtime/threadsafe_fn
